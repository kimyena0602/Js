//변수 종류 
let a=3;
var b=2;
const c=1

//함수를 쓸 땐 앞에 function을 쓴다. 함수는 코드를 묶어주는 역할을 함.
//정의
function myFunction(x){
    let temo = 2*x + 3 //temp 라는 변수를 만들어 식의 값을 temp에 넣는다.
    return temp
}

//실행
myFunction(1)

//정의
function add (x,y) {
    let temp = x+y;
    return temp
}

//실행
add(1,2)
